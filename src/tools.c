void *supprElemList(void *address, int nb_elem, int type_size, int i) {
    if (nb_elem == 0) {
        printf("Erreur [supprElemList], la liste ");
        return;
    }
    void *res = malloc(nb_elem * type_size);
    for (int j = 0; j < nb_elem; j++)
    return res;
}